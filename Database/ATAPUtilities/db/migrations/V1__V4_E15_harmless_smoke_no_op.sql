-- V4-E15 harmless smoke migration.
-- Intentionally performs no schema or data changes; it exists only to prove the
-- database package BuildMaster pipeline can carry a Flyway migration artifact.
PRINT 'V4-E15 harmless smoke migration executed.';
