USE ;

GO IF OBJECT_ID(N'AceCommander.UserDesignerSettings', N'U') IS NOT NULL
DROP TABLE .;

GO
CREATE TABLE . (
   INT IDENTITY(1, 1) NOT NULL PRIMARY KEY,
    NOT NULL,
   NVARCHAR(40) NOT NULL,
    NULL,
    NULL,
   NVARCHAR(64) NULL,
   INT NOT NULL CONSTRAINT  DEFAULT 0,
   INT NOT NULL CONSTRAINT  DEFAULT 0,
   INT NOT NULL CONSTRAINT  DEFAULT 100,
   NVARCHAR() NULL,
   DATETIME2 NOT NULL CONSTRAINT  DEFAULT SYSUTCDATETIME(),
  CONSTRAINT  UNIQUE (, , )
);

GO
