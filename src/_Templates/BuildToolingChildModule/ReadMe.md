# BuildTooling child module template

This directory is the source template for `New-BuildToolingChildModule`. The generator replaces the module name and applies the approved `ModuleFamily.psd1` member metadata.

## Functional area

PowerShell Build & Packaging - START HERE: `SolutionDocumentation/PowerShell-Modules-Build-Process.md`.
