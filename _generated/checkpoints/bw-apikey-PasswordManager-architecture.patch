diff --git a/SolutionDocumentation/NewComputerSetup.md b/SolutionDocumentation/NewComputerSetup.md
index beb28e7..471da15 100644
--- a/SolutionDocumentation/NewComputerSetup.md
+++ b/SolutionDocumentation/NewComputerSetup.md
@@ -535,11 +535,13 @@ Work through these steps in order. Tick each box as you complete it.
 
 - [ ] **9.4.1** Create and ACL `C:\ProgramData\ATAP\BitwardenCredentials\SvcBuildmaster\`
 - [ ] **9.4.2** Launch a PowerShell session as `SvcBuildmaster` and create its
-      `BW_Login_Credential.xml` and `BW_Unlock_Credential.xml`
+      `BW_Login_Credential.xml`, `BW_Unlock_Credential.xml`, and
+      `BW_ApiKey_Credential.xml`
 - [ ] **9.4.3** Validate the BuildMaster credential files were created
 - [ ] **9.4.4** Create and ACL `C:\ProgramData\ATAP\BitwardenCredentials\SvcProGet\`
 - [ ] **9.4.5** Launch a PowerShell session as `SvcProGet` and create its
-      `BW_Login_Credential.xml` and `BW_Unlock_Credential.xml`
+      `BW_Login_Credential.xml`, `BW_Unlock_Credential.xml`, and
+      `BW_ApiKey_Credential.xml`
 - [ ] **9.4.6** Validate the ProGet credential files were created
 - [ ] **9.4.6.5** Grant `SeBatchLogonRight` ("Log on as a batch job") to
       `SvcBuildmaster` and `SvcProGet` — required before Task Scheduler will
@@ -599,6 +601,15 @@ inside that shell. The helper function takes the new Bitwarden passwords as
 guard that verifies the current user equals `-ServiceAccount`, and wraps
 `Get-BitWardenCredential -Replace` with a single call.
 
+In addition to the login and unlock passwords, provisioning now captures the account's
+**Bitwarden personal API key** and writes a third DPAPI file, `BW_ApiKey_Credential.xml`.
+The startup task (9.4.7) logs in with `bw login --apikey`, which is **exempt from
+two-factor authentication**; email/password login is 2FA-gated and would block on an
+interactive code prompt under an unattended task. Obtain the key from the service
+account's Bitwarden web vault: **Settings → Security → Keys → View API Key**, which
+yields a `client_id` (of the form `user.<guid>`) and a `client_secret`. Have both values
+ready before running the block below — the spawned window prompts for them.
+
 Run **from the elevated administrative session**:
 
 ```powershell
@@ -643,19 +654,23 @@ Write-Host '-----------------------------------------'
 
 . '__UPDATE__'
 
-$bwUser   = Read-Host 'Bitwarden username/email bound to __SERVICE__'
-$bwLogin  = Read-Host 'Bitwarden login password' -AsSecureString
-$bwUnlock = Read-Host 'Bitwarden unlock/master password' -AsSecureString
+$bwUser      = Read-Host 'Bitwarden username/email bound to __SERVICE__'
+$bwLogin     = Read-Host 'Bitwarden login password' -AsSecureString
+$bwUnlock    = Read-Host 'Bitwarden unlock/master password' -AsSecureString
+$bwApiId     = Read-Host 'Bitwarden API key client_id for __SERVICE__'
+$bwApiSecret = Read-Host 'Bitwarden API key client_secret' -AsSecureString
 
 # Splat the arguments so PowerShell binds them by name from a hashtable. This avoids the
 # multi-line command-line parameter binding issue that was converting $bwLogin to String.
 $params = @{
-    ServiceAccount          = '__SERVICE__'
-    CredentialDirectory     = '__CREDDIR__'
-    BitwardenUserName       = $bwUser
-    BitWardenLoginPassword  = $bwLogin
-    BitWardenUnlockPassword = $bwUnlock
-    NoRefresh               = $true
+    ServiceAccount           = '__SERVICE__'
+    CredentialDirectory      = '__CREDDIR__'
+    BitwardenUserName        = $bwUser
+    BitWardenLoginPassword   = $bwLogin
+    BitWardenUnlockPassword  = $bwUnlock
+    BitWardenApiClientId     = $bwApiId
+    BitWardenApiClientSecret = $bwApiSecret
+    NoRefresh                = $true
 }
 Update-ServiceAccountBWCredentialFile @params
 
@@ -693,9 +708,10 @@ elevate. That happens on hardened workstations where the launching session lacks
 - **Task Scheduler one-shot**: register a task with `-Principal SvcBuildmaster -LogonType Password`, trigger once, then `Unregister-ScheduledTask` after the credential files appear.
 
 A new PowerShell window appears running as `SvcBuildmaster`. The window prompts for
-the three Bitwarden values, calls `Update-ServiceAccountBWCredentialFile`, and prints
-a `PSCustomObject` summarizing the rotation. After reviewing the output, close the
-window with `exit`.
+the five Bitwarden values (login email, login password, unlock/master password, API
+key client_id, API key client_secret), calls `Update-ServiceAccountBWCredentialFile`,
+and prints a `PSCustomObject` summarizing the rotation. After reviewing the output,
+close the window with `exit`.
 
 > **Why `-NoRefresh`?** `Update-ServiceAccountBWCredentialFile` normally triggers
 > `Refresh-BWSession -ForceReunlock` at the end so a running service picks up the new
@@ -714,11 +730,12 @@ Get-ChildItem -LiteralPath $credentialDirectory -Filter '*.xml' |
   Select-Object Name, Length, LastWriteTime
 ```
 
-You should see at least these two files:
+You should see at least these three files:
 
 ```text
 <COMPUTERNAME>_SvcBuildmaster_BW_Login_Credential.xml
 <COMPUTERNAME>_SvcBuildmaster_BW_Unlock_Credential.xml
+<COMPUTERNAME>_SvcBuildmaster_BW_ApiKey_Credential.xml
 ```
 
 Confirm via ACL that only `SvcBuildmaster`, `SYSTEM`, and `Administrators` are
@@ -803,17 +820,21 @@ Write-Host '-----------------------------------------'
 
 . '__UPDATE__'
 
-$bwUser   = Read-Host 'Bitwarden username/email bound to __SERVICE__'
-$bwLogin  = Read-Host 'Bitwarden login password' -AsSecureString
-$bwUnlock = Read-Host 'Bitwarden unlock/master password' -AsSecureString
+$bwUser      = Read-Host 'Bitwarden username/email bound to __SERVICE__'
+$bwLogin     = Read-Host 'Bitwarden login password' -AsSecureString
+$bwUnlock    = Read-Host 'Bitwarden unlock/master password' -AsSecureString
+$bwApiId     = Read-Host 'Bitwarden API key client_id for __SERVICE__'
+$bwApiSecret = Read-Host 'Bitwarden API key client_secret' -AsSecureString
 
 $params = @{
-    ServiceAccount          = '__SERVICE__'
-    CredentialDirectory     = '__CREDDIR__'
-    BitwardenUserName       = $bwUser
-    BitWardenLoginPassword  = $bwLogin
-    BitWardenUnlockPassword = $bwUnlock
-    NoRefresh               = $true
+    ServiceAccount           = '__SERVICE__'
+    CredentialDirectory      = '__CREDDIR__'
+    BitwardenUserName        = $bwUser
+    BitWardenLoginPassword   = $bwLogin
+    BitWardenUnlockPassword  = $bwUnlock
+    BitWardenApiClientId     = $bwApiId
+    BitWardenApiClientSecret = $bwApiSecret
+    NoRefresh                = $true
 }
 Update-ServiceAccountBWCredentialFile @params
 
@@ -839,7 +860,7 @@ The header in the spawned window must show `Windows identity : UTAT022\SvcProGet
 If it shows your interactive user, fall back to PsExec or a one-shot scheduled task
 as described under 9.4.2.
 
-Answer the three prompts in the new window, review the rotation summary, then close
+Answer the five prompts in the new window, review the rotation summary, then close
 the window with `exit`. The `-NoRefresh`, `-NoProfile`, and TEMP-reset rationales from
 9.4.2 apply here as well.
 
@@ -1027,7 +1048,11 @@ Expected — two `SymbolicLink` lines pointing at the same worktree path.
 
 The startup task runs once at host boot under each service account, decrypts the DPAPI
 credential files, and writes `BW_SESSION` into that account's User-scope environment so
-the service inherits it on next start.
+the service inherits it on next start. When the CLI is not already authenticated it logs
+in with `bw login --apikey` (using the `BW_ApiKey` credential from 9.4.2/9.4.5), then
+unlocks the vault; API-key login is used because it is exempt from two-factor auth,
+whereas email/password login would block on a 2FA prompt and hang the unattended task.
+After a fresh login the task runs `bw sync` once to populate the local vault cache.
 
 ```powershell
 # Resolve the init script using the same candidate-roots fallback as 9.4.2 / 9.4.5
@@ -1141,8 +1166,9 @@ If the second call returns the expected username string, the manual provisioning
 that service account is complete.
 
 > **Cross-reference:** Re-keying these credential files later (Bitwarden master password
-> change, Windows password rotation, host migration) is handled by
-> `Update-ServiceAccountBWCredentialFile`. See
+> change, Windows password rotation, **API-key regeneration**, host migration) is handled
+> by `Update-ServiceAccountBWCredentialFile` — pass `-BitWardenApiClientId` /
+> `-BitWardenApiClientSecret` to rotate the API key alongside the passwords. See
 > [ServiceAccountsAndBitwarden.md](ServiceAccountsAndBitwarden.md#rotation-and-refresh-strategy)
 > for the rotation runbook.
 
diff --git a/src/ATAP.Utilities.BuildTooling.PowerShell/public/Initialize-ServiceAccountBitwardenSession.ps1 b/src/ATAP.Utilities.BuildTooling.PowerShell/public/Initialize-ServiceAccountBitwardenSession.ps1
index 4091015..9d68add 100644
--- a/src/ATAP.Utilities.BuildTooling.PowerShell/public/Initialize-ServiceAccountBitwardenSession.ps1
+++ b/src/ATAP.Utilities.BuildTooling.PowerShell/public/Initialize-ServiceAccountBitwardenSession.ps1
@@ -8,15 +8,20 @@ Initialize-BitwardenSession in LoginScript.ps1. It is registered as a per-servic
 Task Scheduler job that runs `At Startup` under the owning service account's identity.
 
 The script:
-1. Reads the DPAPI-encrypted login + unlock credential files for the running service
+1. Reads the DPAPI-encrypted unlock + API-key credential files for the running service
    account from the configured credential directory.
-2. Writes the master/unlock password to a short-lived clear-text file inside the
+2. If the CLI is not authenticated, logs in with `bw login --apikey` using the API-key
+   credential's client_id/client_secret (BW_CLIENTID / BW_CLIENTSECRET). API-key login
+   is exempt from two-factor auth; email/password login is 2FA-gated and would block on
+   an interactive prompt under an unattended task, so it is not used here.
+3. Writes the master/unlock password to a short-lived clear-text file inside the
    protected credential directory and calls `bw unlock --raw --passwordfile <path>`.
    The temp file is deleted in a finally block; the directory ACL ensures only the
    owning service account, SYSTEM, and Administrators can read it.
-3. Writes the resulting `BW_SESSION` to the **User scope** for the service account so
-   subsequent processes started as that account inherit a valid session.
-4. Logs each phase via PSFramework and writes start/success/failure events to the
+4. Writes the resulting `BW_SESSION` to the **User scope** for the service account so
+   subsequent processes started as that account inherit a valid session, and runs
+   `bw sync` after a fresh login to populate the local vault cache.
+5. Logs each phase via PSFramework and writes start/success/failure events to the
    Windows Application Event Log (source `ATAPServiceAccountBW`).
 
 The script must be invoked **as the service account itself** (Task Scheduler 'Run As',
@@ -29,10 +34,6 @@ Conventional location:
   C:\ProgramData\ATAP\BitwardenCredentials\<ServiceAccount>\
 Required.
 
-.PARAMETER BitwardenUserName
-Optional override for the Bitwarden login email. When omitted, the username is read
-from the DPAPI login credential file.
-
 .OUTPUTS
 PSCustomObject with Success (bool) and Message (string) properties.
 
@@ -52,10 +53,7 @@ function Initialize-ServiceAccountBitwardenSession {
   param(
     [Parameter(Mandatory = $true, Position = 0, ValueFromPipelineByPropertyName = $true)]
     [ValidateNotNullOrWhiteSpace()]
-    [string]$CredentialDirectory,
-
-    [Parameter(Mandatory = $false, ValueFromPipelineByPropertyName = $true)]
-    [string]$BitwardenUserName
+    [string]$CredentialDirectory
   )
 
   BEGIN {
@@ -112,30 +110,45 @@ function Initialize-ServiceAccountBitwardenSession {
         # 3. Read DPAPI-protected credentials. Running user must own these files.
         Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Verbose -Message "Reading DPAPI credentials from '$CredentialDirectory'" -Tag 'serviceaccount-bw'
         $credentials = Get-BitWardenCredential -CredentialDirectory $CredentialDirectory
-        $loginCredential = $credentials['LoginCredential']
         $unlockCredential = $credentials['UnlockCredential']
+        $apiKeyCredential = $credentials['ApiKeyCredential']
 
-        $loginEmail = if ([string]::IsNullOrWhiteSpace($BitwardenUserName)) { $loginCredential.UserName } else { $BitwardenUserName }
-        $loginPassword = $loginCredential.GetNetworkCredential().Password
         $unlockPassword = $unlockCredential.GetNetworkCredential().Password
 
-        # 4. Login if needed.
+        # 4. Login if needed — via API key. Email/password login is the one Bitwarden
+        #    auth path that is subject to 2FA; with 2FA enabled it blocks on an
+        #    interactive code prompt and hangs an unattended task. API-key login
+        #    (BW_CLIENTID / BW_CLIENTSECRET) is exempt from 2FA and is the supported
+        #    unattended path. The vault remains locked after login, so step 5 still
+        #    performs the unlock.
+        $freshLogin = $false
         $statusOutput = & bw status 2>&1
         $status = $null
         try { $status = $statusOutput | ConvertFrom-Json -ErrorAction Stop } catch { $status = $null }
         $isLoggedIn = $status -and $status.status -ne 'unauthenticated'
 
         if (-not $isLoggedIn) {
-          Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Verbose -Message "Calling bw login for '$loginEmail'" -Tag 'serviceaccount-bw'
-          $env:BW_PASSWORD = $loginPassword
-          $loginOutput = & bw login $loginEmail --passwordenv BW_PASSWORD 2>&1
-          $loginExit = $LASTEXITCODE
-          Remove-Item Env:BW_PASSWORD -ErrorAction SilentlyContinue
+          if (-not $apiKeyCredential) {
+            $msg = 'Not logged in and no BW_ApiKey credential file present. Unattended login requires a Bitwarden personal API key (email/password login is 2FA-gated and cannot run unattended). Provision it via Update-ServiceAccountBWCredentialFile (see NewComputerSetup.md section 9.4.2).'
+            Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Error -Message $msg -Tag 'serviceaccount-bw'
+            return [PSCustomObject]@{ Success = $false; Message = $msg }
+          }
+          Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Verbose -Message "Calling bw login --apikey (client_id: $($apiKeyCredential.UserName))" -Tag 'serviceaccount-bw'
+          $env:BW_CLIENTID = $apiKeyCredential.UserName
+          $env:BW_CLIENTSECRET = $apiKeyCredential.GetNetworkCredential().Password
+          try {
+            $loginOutput = & bw login --apikey 2>&1
+            $loginExit = $LASTEXITCODE
+          }
+          finally {
+            Remove-Item Env:BW_CLIENTID, Env:BW_CLIENTSECRET -ErrorAction SilentlyContinue
+          }
           if ($loginExit -ne 0) {
-            $msg = "bw login failed (exit $loginExit). Output: $loginOutput"
+            $msg = "bw login --apikey failed (exit $loginExit). Output: $loginOutput"
             Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Error -Message $msg -Tag 'serviceaccount-bw'
             return [PSCustomObject]@{ Success = $false; Message = $msg }
           }
+          $freshLogin = $true
         }
 
         # 5. Materialize the short-lived password file inside the protected credential
@@ -148,7 +161,6 @@ function Initialize-ServiceAccountBitwardenSession {
         $unlockExit = $LASTEXITCODE
 
         $unlockPassword = $null
-        $loginPassword = $null
 
         $sessionKeyStr = if ($sessionKey) { $sessionKey.ToString() } else { $null }
 
@@ -156,6 +168,18 @@ function Initialize-ServiceAccountBitwardenSession {
           $env:BW_SESSION = $sessionKeyStr
           [System.Environment]::SetEnvironmentVariable('BW_SESSION', $sessionKeyStr, 'User')
           Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Important -Message 'Bitwarden vault unlocked and BW_SESSION stored in User scope' -Tag 'serviceaccount-bw'
+
+          # After a fresh API-key login the local vault cache may be empty/stale, so a
+          # secret lookup can miss until the first sync. Sync once here. An existing
+          # session already has a populated cache, so this is skipped on refresh. The
+          # sync is best-effort: a non-zero exit is logged but does not fail the unlock.
+          if ($freshLogin) {
+            Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Verbose -Message 'Running bw sync after fresh login to populate the local vault cache' -Tag 'serviceaccount-bw'
+            $syncOutput = & bw sync --session $sessionKeyStr 2>&1
+            if ($LASTEXITCODE -ne 0) {
+              Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Important -Message "bw sync returned exit $LASTEXITCODE (non-fatal): $syncOutput" -Tag 'serviceaccount-bw'
+            }
+          }
           return [PSCustomObject]@{ Success = $true; Message = 'BW_SESSION established for service account' }
         }
         else {
diff --git a/src/ATAP.Utilities.BuildTooling.PowerShell/public/Update-ServiceAccountBWCredentialFile.ps1 b/src/ATAP.Utilities.BuildTooling.PowerShell/public/Update-ServiceAccountBWCredentialFile.ps1
index ff3a945..2d6b484 100644
--- a/src/ATAP.Utilities.BuildTooling.PowerShell/public/Update-ServiceAccountBWCredentialFile.ps1
+++ b/src/ATAP.Utilities.BuildTooling.PowerShell/public/Update-ServiceAccountBWCredentialFile.ps1
@@ -39,6 +39,16 @@ SecureString containing the new Bitwarden login password. Required.
 .PARAMETER BitWardenUnlockPassword
 SecureString containing the new Bitwarden master/unlock password. Required.
 
+.PARAMETER BitWardenApiClientId
+Bitwarden personal API key client_id ('user.<guid>'). Optional. When supplied together
+with BitWardenApiClientSecret, the rotation also writes the DPAPI 'BW_ApiKey' credential
+file used by unattended `bw login --apikey` (which is exempt from 2FA, unlike email/
+password login). Omit both to rotate only the login/unlock files.
+
+.PARAMETER BitWardenApiClientSecret
+SecureString containing the Bitwarden personal API key client_secret. Optional. Paired
+with BitWardenApiClientId.
+
 .PARAMETER NoRefresh
 Switch. Skip the post-rotation Refresh-BWSession step. Use when the live session must
 be manually reset later (e.g. when the operator will reboot the host).
@@ -90,6 +100,12 @@ function Update-ServiceAccountBWCredentialFile {
     [ValidateNotNull()]
     [System.Security.SecureString]$BitWardenUnlockPassword,
 
+    [Parameter(Mandatory = $false)]
+    [string]$BitWardenApiClientId,
+
+    [Parameter(Mandatory = $false)]
+    [System.Security.SecureString]$BitWardenApiClientSecret,
+
     [Parameter(Mandatory = $false)]
     [switch]$NoRefresh
   )
@@ -146,19 +162,30 @@ function Update-ServiceAccountBWCredentialFile {
         # 4. Decode SecureStrings to plain text only for the duration of the call.
         $loginPlain = [System.Net.NetworkCredential]::new('', $BitWardenLoginPassword).Password
         $unlockPlain = [System.Net.NetworkCredential]::new('', $BitWardenUnlockPassword).Password
+        $apiSecretPlain = if ($BitWardenApiClientSecret) { [System.Net.NetworkCredential]::new('', $BitWardenApiClientSecret).Password } else { $null }
 
         try {
           Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Verbose -Message "Calling Get-BitWardenCredential -Replace for '$ServiceAccount'"
-          $null = Get-BitWardenCredential `
-            -CredentialDirectory $CredentialDirectory `
-            -BitWardenUserName $BitwardenUserName `
-            -BitWardenLoginPassword $loginPlain `
-            -BitWardenUnlockPassword $unlockPlain `
-            -Replace
+          $getBwParams = @{
+            CredentialDirectory     = $CredentialDirectory
+            BitWardenUserName       = $BitwardenUserName
+            BitWardenLoginPassword  = $loginPlain
+            BitWardenUnlockPassword = $unlockPlain
+            Replace                 = $true
+          }
+          # Thread the API key through only when both halves are supplied, so a
+          # login/unlock-only rotation leaves any existing API-key file untouched.
+          if (-not [string]::IsNullOrWhiteSpace($BitWardenApiClientId) -and -not [string]::IsNullOrWhiteSpace($apiSecretPlain)) {
+            Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Verbose -Message "Including API-key credential (client_id: $BitWardenApiClientId) in rotation"
+            $getBwParams['BitWardenApiClientId']     = $BitWardenApiClientId
+            $getBwParams['BitWardenApiClientSecret'] = $apiSecretPlain
+          }
+          $null = Get-BitWardenCredential @getBwParams
         }
         finally {
           $loginPlain = $null
           $unlockPlain = $null
+          $apiSecretPlain = $null
         }
 
         # 5. Identify newly created .bak files.
diff --git a/src/ATAP.Utilities.Security.Powershell/public/Get-BitWardenCredential.ps1 b/src/ATAP.Utilities.Security.Powershell/public/Get-BitWardenCredential.ps1
index 12f599f..43864c0 100644
--- a/src/ATAP.Utilities.Security.Powershell/public/Get-BitWardenCredential.ps1
+++ b/src/ATAP.Utilities.Security.Powershell/public/Get-BitWardenCredential.ps1
@@ -23,6 +23,15 @@ Username label for unlock credential (typically 'BitWarden'). Required when crea
 .PARAMETER BitWardenUnlockPassword
 Master password for unlocking the vault. Required when creating new credentials.
 
+.PARAMETER BitWardenApiClientId
+Bitwarden personal API key client_id (e.g. 'user.<guid>'). Optional. When supplied
+together with BitWardenApiClientSecret during creation, an additional DPAPI-encrypted
+'BW_ApiKey' credential file is written. Unattended `bw login --apikey` (2FA-exempt)
+uses this; email/password login is subject to 2FA and cannot run unattended.
+
+.PARAMETER BitWardenApiClientSecret
+Bitwarden personal API key client_secret. Optional. Paired with BitWardenApiClientId.
+
 .PARAMETER Replace
 Forces recreation of the credential files by prompting for passwords even if files exist.
 The existing credential files will be backed up before being replaced.
@@ -73,6 +82,14 @@ function Get-BitWardenCredential {
     [ValidateNotNullOrEmpty()]
     [string]$BitWardenUnlockPassword,
 
+    [Parameter(Mandatory = $false)]
+    [ValidateNotNullOrEmpty()]
+    [string]$BitWardenApiClientId,
+
+    [Parameter(Mandatory = $false)]
+    [ValidateNotNullOrEmpty()]
+    [string]$BitWardenApiClientSecret,
+
     [Parameter(Mandatory = $false)]
     [switch]$Replace
   )
@@ -125,11 +142,14 @@ function Get-BitWardenCredential {
     $currentSamName = ([System.Security.Principal.WindowsIdentity]::GetCurrent().Name -split '\\') | Select-Object -Last 1
     $loginCredFileName = "$env:COMPUTERNAME`_$currentSamName`_BW_Login_Credential.xml"
     $unlockCredFileName = "$env:COMPUTERNAME`_$currentSamName`_BW_Unlock_Credential.xml"
+    $apiKeyCredFileName = "$env:COMPUTERNAME`_$currentSamName`_BW_ApiKey_Credential.xml"
     $loginCredPath = Join-Path $CredentialDirectory $loginCredFileName
     $unlockCredPath = Join-Path $CredentialDirectory $unlockCredFileName
+    $apiKeyCredPath = Join-Path $CredentialDirectory $apiKeyCredFileName
 
     Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Debug -Message "Login credential file path: $loginCredPath"
     Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Debug -Message "Unlock credential file path: $unlockCredPath"
+    Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Debug -Message "API-key credential file path: $apiKeyCredPath"
   }
 
   PROCESS {
@@ -138,6 +158,7 @@ function Get-BitWardenCredential {
       $shouldCreateNew = $false
       $loginCredential = $null
       $unlockCredential = $null
+      $apiKeyCredential = $null
 
       # Check if both credential files exist
       $loginExists = Test-Path $loginCredPath
@@ -213,6 +234,22 @@ function Get-BitWardenCredential {
         Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Debug -Message "Saving encrypted unlock credential to: $unlockCredPath"
         $unlockCredential | Export-Clixml -Path $unlockCredPath -ErrorAction Stop
 
+        # Create the API-key credential only when both values are supplied. This keeps
+        # login/unlock-only rotations from disturbing an existing API-key file.
+        if (-not [string]::IsNullOrWhiteSpace($BitWardenApiClientId) -and -not [string]::IsNullOrWhiteSpace($BitWardenApiClientSecret)) {
+          if ($Replace -and (Test-Path $apiKeyCredPath)) {
+            $apiKeyBackupPath = "$apiKeyCredPath.$(Get-Date -Format 'yyyyMMdd_HHmmss').bak"
+            Copy-Item -Path $apiKeyCredPath -Destination $apiKeyBackupPath -Force -ErrorAction Stop
+            Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Verbose -Message "Backed up existing API-key credential to: $apiKeyBackupPath"
+          }
+          Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Debug -Message "Creating API-key credential for client_id: $BitWardenApiClientId"
+          $secureApiSecret = ConvertTo-SecureString $BitWardenApiClientSecret -AsPlainText -Force
+          $apiKeyCredential = New-Object System.Management.Automation.PSCredential($BitWardenApiClientId, $secureApiSecret)
+          Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Debug -Message "Saving encrypted API-key credential to: $apiKeyCredPath"
+          $apiKeyCredential | Export-Clixml -Path $apiKeyCredPath -ErrorAction Stop
+          Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Important -Message "Successfully saved API-key credential for client_id: $BitWardenApiClientId"
+        }
+
         if ($Replace) {
           Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Important -Message "Successfully replaced and saved credentials for login user: $($loginCredential.UserName)"
         }
@@ -221,10 +258,19 @@ function Get-BitWardenCredential {
         }
       }
 
-      # Return hashtable with both credentials
+      # Ensure the returned API-key credential reflects what is on disk when it was not
+      # just created above (plain load, or a login/unlock-only rotation). Absent file
+      # leaves ApiKeyCredential null so existing callers are unaffected.
+      if (-not $apiKeyCredential -and (Test-Path $apiKeyCredPath)) {
+        $apiKeyCredential = Import-Clixml -Path $apiKeyCredPath -ErrorAction Stop
+        Write-PSFMessage -FunctionName $fn -ModuleName $mn -Level Verbose -Message "Loaded existing API-key credential for client_id: $($apiKeyCredential.UserName)"
+      }
+
+      # Return hashtable with the login, unlock, and (optional) API-key credentials
       $credentialHashtable = @{
         LoginCredential  = $loginCredential
         UnlockCredential = $unlockCredential
+        ApiKeyCredential = $apiKeyCredential
       }
 
       return $credentialHashtable