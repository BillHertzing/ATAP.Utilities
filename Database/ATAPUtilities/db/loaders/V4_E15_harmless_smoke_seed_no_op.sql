-- V4-E15 harmless smoke seed loader.
-- Intentionally idempotent and side-effect free; the CSV seed is packaged as
-- evidence input while this loader gives rehearsal tooling a no-op loader file.
PRINT 'V4-E15 harmless smoke seed loader executed.';
